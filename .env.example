# Environment (local, development, production, test)
NODE_ENV=

# Server configuration
HOST=          # e.g. 0.0.0.0 or localhost
PORT=          # e.g. 3000

# Database connection string
DATABASE_URL=  # e.g. postgres://user:pass@host:port/dbname

# JWT configuration
JWT_SECRET=    # your JWT signing secret
JWT_TTL=       # token time-to-live (e.g. "1h", "3600s")
